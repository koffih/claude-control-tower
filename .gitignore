node_modules/
dist/
coverage/
*.tsbuildinfo

.DS_Store
Thumbs.db

.env
.env.*
!.env.example

.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

*.log
npm-debug.log*
.npm/
.eslintcache
packages/cli/README.md

# Local scratch. Never committed: this is where credentials and notes get parked,
# and a public repository is exactly the wrong place for either.
tmp/
*.token
.env.local

# Local push helper. It reads a GitHub token, so it stays out of the repository.
push.py
